package Model.Animals;

public abstract class Predator extends Animal {
    /*public void kill(int x, int y) {
        for (int i = Math.max(0, x); i < Math.min(x, MAP_SIZE); i++) {
            for (int j = Math.max(0, y); j < Math.min(x, MAP_SIZE); j++) {
                Cell cell = cells.get(x).get(y);
                Prey prey = cell.getPrey();
                if (prey != null) {
                    cell.discardKilledPrey(prey);
                }
            }
        }
    }*/
}
