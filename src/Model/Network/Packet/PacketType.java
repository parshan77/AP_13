package Model.Network.Packet;

public enum PacketType {
    SendName,
    buyFromServer,
    checkMoneyForBuy,
    sellProduct,
    donateProducts,
    sendPvMessage,
    friendRequest,
    publicMessage,
    getOnlinePlayers,
    checkUsernameValidation;
}
